beaver.annotation:
    resource: '@BackendBundle/Controller'
    type: annotation

backend.modules:
    resource: 'routes/modules.yaml'

logout:
    path: /logout

backend.page:
    prefix: /ajax
    resource: 'routes/page.ajax.yml'

backend.gallery:
    prefix: /ajax
    resource: 'routes/gallery.ajax.yml'

backend.block:
    prefix: /ajax
    resource: 'routes/block.ajax.yml'

backend.widgets:
    prefix: /ajax
    resource: 'routes/widget.ajax.yml'

backend:
    resource: 'routes/routing.yml'